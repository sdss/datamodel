general:
  name: {{file_species}}
  short: replace me - with a short one sentence summary of file
  description: >-
    replace me - with a longer description of the data product
  environment: {{environment}}
  datatype: {{filetype}}
  filesize: {{filesize}}
  releases: {{releases}}
  naming_convention: >-
    replace me - with {{file_template}} or {{filename}} but with regex pattern matches
  generated_by: >-
    replace me - with the name(s) of any git or svn product(s) that produces this product.

changelog:
  description: this changelog describes changes to the datamodel product and/or file structure

releases:
  WORK:
    template: {{file_template}}
    example: {{example}}
    location: {{location}}
    access: {{access}}
    hdus: {}

