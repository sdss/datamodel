#!/usr/bin/env python3
# encoding: utf-8
#
# @Author: Joel Brownstein <joelbrownstein@sdss.org>
# @Date: Nov 9, 2020
# @Filename: datamodel_install
# @License: BSD 3-Clause
# @Copyright: SDSS.

from datamodel.command import Command, Install

command = Command(name='datamodel_install')
install = Install(options = command.options)
install.set_directory()
install.clone()
install.checkout_branch()
install.set_modulefile()
install.done()

"""
usage via commandline:
datamodel_install -v

usage via python:
from datamodel.command import Install
install = Install(branch = 'test', verbose = True)
install.set_directory()
install.clone()
install.checkout_branch()
install.set_modulefile()
install.done()

"""
