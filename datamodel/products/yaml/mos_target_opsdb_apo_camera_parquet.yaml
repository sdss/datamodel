general:
  name: mos_target_opsdb_apo_camera_parquet
  short: 'MOS Target Table: opsdb_apo_camera'
  description: The table contains the cameras at the observatory. The three APOGEE
    chips are treated as one camera, while the separate blue channel and red channel
    BOSS chips are treated separately. APO version.
  datatype: PARQUET
  filesize: 795 bytes
  releases:
  - DR20
  environments:
  - MOS_TARGET
  surveys:
  - SDSS
  naming_convention: $MOS_TARGET/[V_TARG]/[FTYPE]/mos_opsdb_apo_camera.[FTYPE] with
    FTYPE='parquet' and V_TARG='2.0.0' for DR20.
  generated_by: sdss5db targeting database.
  data_level: 2.3.3
  design: false
  vac: false
  recommended_science_product: false
changelog:
  description: Describes changes to the datamodel product and/or file structure from
    one release to another
  releases: {}
releases:
  DR20:
    template: $MOS_TARGET/[V_TARG]/[FTYPE]/mos_opsdb_apo_camera.[FTYPE]
    example: 2.0.0/parquet/mos_opsdb_apo_camera.parquet
    location: '{v_targ}/{ftype}/mos_opsdb_apo_camera.{ftype}'
    environment: MOS_TARGET
    access:
      in_sdss_access: true
      path_name: mos_target_opsdb_apo_camera
      path_template: $MOS_TARGET/{v_targ}/{ftype}/mos_opsdb_apo_camera.{ftype}
      path_kwargs:
      - ftype
      - v_targ
      access_string: mos_target_opsdb_apo_camera = $MOS_TARGET/{v_targ}/{ftype}/mos_opsdb_apo_camera.{ftype}
    survey: SDSS
    dataframe:
      columns:
        pk:
          name: pk
          dtype: Int32
          unit: ''
          description: Unique identifier
        label:
          name: label
          dtype: String
          unit: ''
          description: The name of the camera.
      metadata: {}
notes: |-
  None
regrets: |-
  I  have no regrets!
