general:
  name: mos_target_opsdb_apo_camera_frame_parquet
  short: 'MOS Target Table: opsdb_apo_camera_frame'
  description: The table contains signal-to-noise estimates for each 'camera', for
    each exposure. APOGEE is treated as one camera, while R1/2 and B1/2 are treated
    separately. APO version.
  datatype: PARQUET
  filesize: 940 KB
  releases:
  - DR20
  environments:
  - MOS_TARGET
  surveys:
  - SDSS
  naming_convention: $MOS_TARGET/[V_TARG]/parquet/mos_opsdb_apo_camera_frame.parquet
    with V_TARG=2.0.0 for DR20.
  generated_by: sdss5db targeting database.
  data_level: 2.3.3
  design: false
  vac: false
  recommended_science_product: false
changelog:
  description: Describes changes to the datamodel product and/or file structure from
    one release to another
  releases: {}
releases:
  DR20:
    template: $MOS_TARGET/[V_TARG]/parquet/mos_opsdb_apo_camera_frame.parquet
    example: 2.0.0/parquet/mos_opsdb_apo_camera_frame.parquet
    location: '{v_targ}/parquet/mos_opsdb_apo_camera_frame.parquet'
    environment: MOS_TARGET
    access:
      in_sdss_access: true
      path_name: mos_target_opsdb_apo_camera_frame_parquet
      path_template: $MOS_TARGET/{v_targ}/parquet/mos_opsdb_apo_camera_frame.parquet
      path_kwargs:
      - v_targ
      access_string: mos_target_opsdb_apo_camera_frame_parquet = $MOS_TARGET/{v_targ}/parquet/mos_opsdb_apo_camera_frame.parquet
    survey: SDSS
    dataframe:
      columns:
        pk:
          name: pk
          dtype: Int32
          unit: ''
          description: Unique identifier
        exposure_pk:
          name: exposure_pk
          dtype: Int32
          unit: ''
          description: Unique identifier in the exposure table
        camera_pk:
          name: camera_pk
          dtype: Int16
          unit: ''
          description: Unique identifier in the camera table
        sn2:
          name: sn2
          dtype: Float32
          unit: ''
          description: Signal-to-noise squared estimate from on-mountain quick reduction
            pipelines
        comment:
          name: comment
          dtype: String
          unit: ''
          description: An optional comment
      metadata: {}
notes: None
regrets: I  have no regrets!
