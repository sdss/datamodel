general:
  name: mos_target_design_to_field_parquet
  short: 'MOS Target Table: design_to_field'
  description: Connects a design to a given field within a version of the survey.
    Sets the placement of the design in the overall exposure sequence for that field.
  datatype: PARQUET
  filesize: 5 MB
  releases:
  - DR20
  environments:
  - MOS_TARGET
  surveys:
  - SDSS
  naming_convention: $MOS_TARGET/[V_TARG]/[FTYPE]/mos_design_to_field.[FTYPE] with
    FTYPE='parquet' and V_TARG='2.0.0' for DR20.
  generated_by: sdss5db targeting database.
  data_level: 2.3.3
  design: false
  vac: false
  recommended_science_product: false
changelog:
  description: Describes changes to the datamodel product and/or file structure from
    one release to another
  releases: {}
releases:
  DR20:
    template: $MOS_TARGET/[V_TARG]/[FTYPE]/mos_design_to_field.[FTYPE]
    example: 2.0.0/parquet/mos_design_to_field.parquet
    location: '{v_targ}/{ftype}/mos_design_to_field.{ftype}'
    environment: MOS_TARGET
    access:
      in_sdss_access: true
      path_name: mos_target_design_to_field
      path_template: $MOS_TARGET/{v_targ}/{ftype}/mos_design_to_field.{ftype}
      path_kwargs:
      - v_targ
      - ftype
      access_string: mos_target_design_to_field = $MOS_TARGET/{v_targ}/{ftype}/mos_design_to_field.{ftype}
    survey: SDSS
    dataframe:
      columns:
        pk:
          name: pk
          dtype: Int32
          unit: ''
          description: The primary key. A sequential identifier.
        design_id:
          name: design_id
          dtype: Int32
          unit: ''
          description: The primary key of the design in the dr20_design table.
        field_pk:
          name: field_pk
          dtype: Int32
          unit: ''
          description: The primary key of the field in the dr20_field table.
        exposure:
          name: exposure
          dtype: Int64
          unit: ''
          description: The 0-indexed exposure number in the lunation sequence for
            the field.
        field_exposure:
          name: field_exposure
          dtype: Int64
          unit: ''
          description: The 0-indexed overall exposure number in the sequence for the
            field.
      metadata: {}
notes: |-
  None
regrets: |-
  I  have no regrets!
